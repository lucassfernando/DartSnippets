void main(){
  //Temos 6 tipos
  // == igualdade
  // != diferenca
  // > maior que
  // < menor que
  // >= maior ou igual
  // <= menor ou igual

  final tipoPet = 'Gato';

  if(tipoPet != 'Cachorro'){
    print('Desculpe não temos nada para o seu pet');
  } else {
    print('temos ração para cachorro!');
  }
}